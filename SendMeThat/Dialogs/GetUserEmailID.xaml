   <Window x:Class="SendMeThat.GetUserEmailID"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="135" d:DesignWidth="400"
             Height="135"
             Width="400"
             Title="Send Me That!">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="1" Background="#FFE1E1E1" Padding="0">
            <StackPanel >
                <TextBlock Text="Enter your Email ID : " Margin="5" />

                <TextBox x:Name="ResponseTextBox" Margin="5" MaxHeight="100" 
                         />
            </StackPanel>
        </Border>
        <Border Grid.Row="4" Background="#FFE1E1E1" Padding="5">
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="5,10,5,4.8" Padding="5" Click="GetUserEmail">Submit</Button>
            </StackPanel>
        </Border>
    </Grid>

    </Window> 